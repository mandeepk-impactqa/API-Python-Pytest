[tool.black]
line-length = 100
target-version = ["py312"]

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
exclude = ["reports", "logs"]

[[tool.mypy.overrides]]
module = ["allure.*"]
ignore_missing_imports = true
